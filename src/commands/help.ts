export const showHelpMessage = () => {
  console.log(`
        Nano Package Manager (opm)
        
        Usage:
            opm install <package-name> Install a package
            opm help
        `);
};
